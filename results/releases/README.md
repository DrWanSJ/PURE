# results/releases/

Release evidence for versioned releases (first target: D20 v0.1). A release
entry is a curated, self-describing snapshot (manifests, QC, baseline deltas)
that was produced by `scripts/release_preflight.m` in release mode. Nothing is
committed here yet.
