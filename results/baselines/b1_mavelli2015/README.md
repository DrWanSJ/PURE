# Frozen baseline: B1 — Mavelli 2015 literature reference

This directory is the **frozen regression baseline** of the B1 benchmark
(`PURE_literature_reference`, three Fig. 4 DNA conditions). It is committed,
never overwritten, and never extended by ordinary runs (those go to
`results/runs/`).

- `DNA_*/{trajectory.csv,rates.csv,qc.json}`, `benchmark_summary.json`,
  figures — the verified numerical baseline. Every file carries
  `provenance_status = 
