# schemas/

Machine-readable schemas for repository artifacts (planned): model definition,
run manifest, run QC, analysis and flow schemas. Reserved skeleton — no schemas
are defined yet; do not commit speculative placeholders. The de-facto current
schemas are the shipped files themselves:
`models/literature_reference/model_definition.json` and the `manifest.json` /
`qc.json` produced by `matlab/src/simulate/run_fig4_benchmark.m`.
