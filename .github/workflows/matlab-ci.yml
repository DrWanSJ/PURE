# =====================================================================
# DRAFT WORKFLOW — NOT VERIFIED (do not trust until human-validated)
#
# docs/audit/audit_status.md records CI = not_verified. This file has never run:
# MATLAB on GitHub-hosted runners requires a MATLAB license/setup
# (e.g. matlab-actions/setup-matlab + license, or MATLAB Batch for public
# repos). A human must confirm licensing/runner availability, then remove
# this banner and rename the workflow before relying on it.
# Manual local equivalent (verified path):
#   matlab -batch "addpath('scripts'); run_all_tests"
# =====================================================================
name: "[draft, not verified] PURE B1 tests"

on:
  push:
    branches: ["**"]
  workflow_dispatch:

jobs:
  matlab-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up MATLAB (placeholder — requires license/runner decision)
        uses: matlab-actions/setup-matlab@v2
        with:
          release: R2025b

      - name: Run all tests (unit + smoke + preflight)
        uses: matlab-actions/run-command@v2
        with:
          command: addpath('scripts'); run_all_tests

      - name: Upload test logs
        uses: actions/upload-artifact@v4
        with:
          name: b1-test-logs
          path: logs/
          if-no-files-found: error
