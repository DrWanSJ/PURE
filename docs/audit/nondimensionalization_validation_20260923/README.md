# Reduced six-state nondimensionalization audit (2026-09-23)

Overall **FAIL**: the fixed derivative-transform threshold is not met. All trajectory comparisons pass. See the [formal report](../../theory/nondimensionalization_report.md) for the equations, measurements, diagnosis and scope.

- `acceptance_criteria.json`: original fixed criteria; never changed after execution.
- `preregistration.json`: time, source commit, criteria hash and initial implementation hashes before the first numerical run.
- `failed_attempt.json`: numerical JSON extracted from an unchanged diagnostic replay of the first failed implementation; the original runner had a structure-array parser error before its summary write.
- `implementation_revision.json`: pre-rerun hashes documenting the parser correction and extra diagnostics; no production ODE/scaling/solver or threshold change.
- `validation_results.json`: actual final MATLAB TestResult counts and computed metrics, including the failed derivative check. Generated by the runner, not handwritten.
- `regression_results.json`: repository-wide test runner summary (when available); failures remain failures.

The source/pre-validation commit is `b65d5e2a17716ce72dc1b26c41caed6bc4d6ef34`. The incoming checkout already had local commit `80185f0f5c6c4f3b80e6fe24c9ea0fc999518c02` with full 12-state dimensionless work. This audit adds the specifically requested reduced integration and preserves prior captured evidence.

Reproduce from the repository root in MATLAB:

```matlab
addpath('scripts');
run_nondimensionalization_validation;
```

The runner prints real TestResults, verifies registered hashes, and returns an error on any failed/incomplete test. When recorded results already exist, it writes to a new temporary directory, preserving the audit. The derived map is generated by `export_pure_nondim_map` from the unchanged `pure_nondim_map` runtime computation; map equality and its use for transformations are tested. The map is not an independent parameter authority and this audit does not certify D6 completion.

The task's failure rule prevents the second commit and push. Draft implementation and evidence are intentionally left in the worktree for review; there was no force push, branch creation, threshold relaxation, clipping or fitting.
